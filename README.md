# Links

Links is a service that allows you to manage short links, capture user fingerprints, and route users based on their current platform.
It is built using Rust and the Axum web framework, and it leverages a PostgreSQL or SQLite database for data storage.